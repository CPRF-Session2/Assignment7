/*Mariposa Lee, I did not realize you were supposed to make your own strcmp until 10 minutes before the due time. I quickly tried to whip it up. I apologize.*/
#include <stdio.h>
#include <string.h>
int func (char *str1, char *str2, char *a, char *b) {
	a=0;
	b=0;
	int i;
	while (i=0; i!=(strlen(str1))||(strlen(str2));i++) {
		if (str1[a] > str2[b]) {
			i= -1;
			return i;}
	if (str1[a] < str2[b]) {
		i=1;
		return i; }
	if (str1[a] == str2[b]) {
		continue;}
	int q;
	while 
	/*q = strcmp(a, b);*/
return q;
}


int main () {
char s1[100];
char s2[100];
int q;
printf("Please enter two strings.\n");
fgets(s1, sizeof(s1), stdin);
fgets(s2, sizeof(s2), stdin);
q= func(s1, s2);
printf("%d\n", q);


return 0;
}
